name: bank_check_ocr
version: 1.0.0
description: 支持银行支票识别,精准提取支票号码、出票日期、大小写金额及出票人签章等。
author: SCNet
license: MIT
tags:
  - ocr
  - 文字提取
required_env_vars:
  - SCNET_API_KEY

optional_env_vars:
  - SCNET_API_BASE

primary_credential: SCNET_API_KEY

# 增加项目主页，解决 missing homepage 警告
homepage: https://github.com/SCNet-sugon/bank_check_ocr.git  # 请替换为实际仓库地址
dependencies:
  - python3
  - requests